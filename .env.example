# None of these are wired up yet — Phase 1 is frontend only.
# Each one gates a specific integration; the UI states clearly when a
# service is not configured rather than pretending it works.

NEXT_PUBLIC_SITE_URL=http://localhost:3003

# Phase 2 — database / availability
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# Phase 3 — payments
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=

# Phase 4 — AI concierge, email, SMS
ANTHROPIC_API_KEY=
RESEND_API_KEY=
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=

# Phase 5 — analytics
NEXT_PUBLIC_GA4_ID=
